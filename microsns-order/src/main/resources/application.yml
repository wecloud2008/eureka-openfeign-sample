spring:
  application:
    name: microsns-order
eureka:
  client:
    server-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
server:
  port: 8082